import ContactClient from "./ContactClient";

export const metadata = {
  title: "Initiate Transmission | Contact OneFrame Studios",
  description: "Secure a slot in our post-production pipeline. Contact OneFrame Studios to discuss your VFX, editing, or color grading needs.",
  openGraph: {
    title: "Initiate Transmission | Contact OneFrame Studios",
    description: "Secure a slot in our post-production pipeline.",
  }
};

export default function ContactPage() {
  return <ContactClient />;
}
